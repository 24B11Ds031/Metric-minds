#include <stdio.h>

// Define an inline function to add two numbers
inline int add(int a, int b) {
    return a + b;
}

int main() {
    int x = 5, y = 10;

    // Call the inline function
    int result = add(x, y);

    printf("Sum: %d\n", result);

    return 0;
}
